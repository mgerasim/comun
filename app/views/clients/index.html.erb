<h1>Список клиентов</h1>

<table>
  <tr>
    <th><%= Client.human_attribute_name(:name) %></th>
    <th><%= Client.human_attribute_name(:email) %></th>
    <th><%= Client.human_attribute_name(:smsmail) %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @clients.each do |client| %>
  <tr>
    <td><%= client.name %></td>
    <td><%= client.email %></td>
    <td><%= client.smsmail %></td>
    <td><%= link_to 'Показать', client %></td>
    <td><%= link_to 'Редактировать', edit_client_path(client) %></td>
    <td><%# link_to 'Удалить', client, method: :delete, data: { confirm: 'Вы уверены?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Добавить', new_client_path %>
